You are a senior Next.js engineer.
Your task is to generate a complete CRUD UI feature for a Next.js App Router project.
The UI communicates with an existing REST API.
Generate working Next.js code
Follow the instructions exactly and do not invent extra files.

Stack:

daisyUI
- Use DaisyUI components for UI elements (buttons, inputs, cards, modals, alerts).
- Follow DaisyUI component structure and classes.

tailwindcss
- Mobile-first responsive design.
- Use Tailwind utilities for layout, spacing, grid, and responsiveness.
- Prefer DaisyUI components instead of building custom components with Tailwind when possible.

zod
- Use Zod schemas for validation.
- Validate both client-side and server-side using the same schema.

lucide-react
- Use Lucide icons for UI icons.
- Prefer outline icons with consistent sizing.


## INPUTS


RESOURCE API URL(BASE API URL will include the resource path.):
http://localhost:8080/{{resource}}


Single Item JSON Response
Example:
{
	"id": 1,
	"name": "Example",
	"description": "Example text",
	"size": 100
}


## DERIVED PLACEHOLDER VALUES 

Feature Name
Extract from the Resource API URL.
Ex: {{resource}}

Entity Name

{{Entity}} : {{resource}} Singular PascalCase of feature name.
{{ENTITY}} : {{resource}} Singular Camel Case of feature name.

Example (PlaceHolders):
http://localhost:8080/users
{{entities}} = users
{{ENTITY}}  = User
{{ENTITY}}  = user

Replace placeholder wherever required.


Fields
All JSON properties except id.

## API ENDPOINTS
GET    <base_api_url>
GET    <base_api_url>/{id}
POST   <base_api_url>
PUT    <base_api_url>/{id}
DELETE <base_api_url>/{id}


## STRICT PROJECT STRUCTURE

You MUST generate ONLY the following files.
Do NOT add extra folders or files.

app/<entities>/
├── page.jsx                          ← List page (Server)
├── service.js                        ← API fetch functions
├── actions.js                        ← Server Actions + Zod validation
├── components/
│   ├── {{ENTITY}}Table.jsx             ← Table with View/Edit/Delete (Server)
│   ├── {{ENTITY}}Form.jsx              ← Reusable form with useActionState (Client)
│   ├── {{ENTITY}}Details.jsx           ← Detail view with stat cards (Server)
│   └── {{ENTITY}}DeleteButton.jsx      ← Confirm + delete with useTransition (Client)
├── new/
│   └── page.jsx                      ← Create page (Server)
└── [id]/
    ├── page.jsx                      ← Detail page (Server)
    └── edit/
        └── page.jsx                  ← Edit page with bound action (Server)

## FILE RESPONSIBILITIES


### service.js:

Contains API functions using fetch.

Functions required:

getAll{{ENTITY}}()
get{{ENTITY}}ById(id)
create{{ENTITY}}(data)
update{{ENTITY}}(id,data)
delete{{ENTITY}}ById(id)


### actions.js:

Contains Next.js Server Actions.


Rules:
Use "use server".
Use Zod validation.
Return validation errors for forms.
Redirect to "/<feature>" after success.

Ex:
export async function create{{ENTITY}}Action(prevState, formData) {
  const raw = {
    title: formData.get("title"),
  };

  const result = BookmarkSchema.safeParse(raw);
  if (!result.success) {
    return { errors: result.error.flatten().fieldErrors, values : raw };
  }

  await create<feature>(result.data); <- Call Server Function
  redirect("/bookmarks");
}

export async function update{{ENTITY}}Action(id, prevState, formData) {
	//...
	await update{{ENTITY}}(id, result.data);
}

export async function delete{{ENTITY}}Action(id) {
  await delete{{ENTITY}}(id);
  redirect("/<entities>");
}



### components/{{ENTITY}}Table.jsx

It must be Server Component.
Display list of records.
Show all fields.

Include actions:
View: Use Link to navigate
Edit: Use Link to navigate
Delete{{ENTITY}}Button.jsx


### components/{{ENTITY}}Form.jsx

It's the client Component.
Reusable form component.

Used for:
Create page
Edit page

Must use:
useActionState(serverAction)
Display validation errors.
Disable submit button while pending.
It should contain "Cancel button" redirecting /<feature> page.

Ex:
"use client";

import { useActionState } from "react";
import { useFormStatus } from "react-dom";

function SubmitButton() {
  const { pending } = useFormStatus();
  return (
    <button
      type="submit"
      disabled={pending}
    >
      {pending ? "Saving..." : "Save"}
    </button>
  );
}

export default function BookmarkForm({ action, initialData }) {
  const [state, formAction] = useActionState(action, { errors: {}, values: {} });

  // After a failed submission, restore what the user typed.
  // On first render, fall back to initialData (edit page prefill).
  
  const values = {
    title:        state.values?.title        ?? initialData?.title        ?? "",
  };

  return (
    <form action={formAction}>
        <input
          name="title"
          defaultValue={values.title}
          key={values.title}
        />
        {state?.errors?.title && (
          <p>{state.errors.title[0]}</p>
        )}
		<SubmitButton />
    </form>
  );
}


### components/{{ENTITY}}Details.jsx

Display all fields of the entity.
It must be Server Component
Include edit link from "next/link";
Include back link from "next/link";

### components/Delete{{ENTITY}}Button.jsx

It's Server Component

"use client"
import { useTransition } from "react";
import { delete{{ENTITY}}Action } from "../actions";

export default function DeleteButton({ id }) {
    const [isPending, startTransition] = useTransition();
    function handleDelete(id) {
        if (!confirm("Delete this bookmark?")) return;
        startTransition(() => deleteBookmarkAction(id));
    }
    return (<button
        onClick={() => handleDelete(id)}
        disabled={isPending}
    >
        Delete
    </button>);
}

### new/page.jsx

Render create page.
It should be Server Component
Render Form component.
Pass create server action.

Ex:
import {{ENTITY}}Form from "../components/{{ENTITY}}Form";
import { create{{ENTITY}}Action } from "../actions";

export default function New{{ENTITY}}Page() {
  return (
      <{{ENTITY}}Form action={create{{ENTITY}}Action} />
  );
}

### [id]/page.jsx

It should  Render details page.
Fetch record by id.
Render Details component.

Ex:
export default async function {{ENTITY}}DetailsPage({ params }) {
  const { id } = await params;
  const {{ENTITY}} = await get{{ENTITY}}ById(id);
  return (
      <{{ENTITY}}Details {{ENTITY}}={{{ENTITY}}} />
  );
}



### [id]/edit/page.jsx

Render edit page.
Fetch record.
Render Form component with initial data.
Pass update server action.

Ex:
export default async function Edit{{ENTITY}}Page({ params }) {
  const { id } = await params;  
  const {{ENTITY}} = await get{{ENTITY}}ById(id);
  const boundAction = update{{ENTITY}}Action.bind(null, id);  

  return <{{ENTITY}}Form action={boundAction} initialData={{{ENTITY}}} />;
}


## IMPLEMENTATION RULES

Use Next.js App Router.
Use React Server Components for pages.
Use fetch for API calls.
Use Tailwind utility classes for minimal styling.
Use explicit imports.
Do NOT use wildcard imports.

## OUTPUT FORMAT

Generate files in this format:
FILE: <path>
<code>
Example:

5. All files must contain complete working code.
6. Follow the file structure exactly.

## FINAL STEP

First display the detected values:

Feature Name
Entity Name
Fields

Then generate all files.
