server:
  port: 9090

spring:
  profiles:
    active:
#    - h2db
    - mysqldb
#    - mysqldb-office  

springdoc:
  show-actuator: true
  # Ensures that data rest repositories are detected
  enable-data-rest: true